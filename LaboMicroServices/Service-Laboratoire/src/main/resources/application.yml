server:
  port: 8087
spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/serviceLaboratoire
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
