server:
  port: 8085
spring:
  application:
    name: customer
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/serviceUtilisateurs
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true